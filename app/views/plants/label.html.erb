<script>
  var doc = new jsPDF('l', 'in', [3, 1]);
  
  doc.setFontSize(22);
  doc.text(1, 0.6, 'Bin : <%= plant.id %>');

  var imgData = '<%= j(to_data_url(plant.datamatrix)) %>'
  doc.addImage(imgData, 'PNG', 0.1, 0.1, 0.8, 0.8);
  
  doc.save('label.pdf');
</script>