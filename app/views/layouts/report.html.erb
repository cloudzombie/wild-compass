<!DOCTYPE html>
<html>
  <head>
    <title>THC-ICS <%= controller_name.titleize %> Report</title>
    <style>
      #report-body table {
        width: 100%;
        text-align: center;
        border: 1px solid black;
        border-collapse: collapse;
      }

      #report-body tr, #report-body th, #report-body td {
        padding-left: 12px;
        padding-right: 12px;
        padding-top: 2px;
        padding-bottom: 2px;
        text-align: center;
        border: 1px solid black;
      }

      #report-header table {
        width: 100%;
        text-align: left;
      }

      #report-header {
        padding-top: 50px;
        padding-bottom: 50px;
      }
    </style>
  </head>

  <body>
    <section id="report-header">
      <table>
        <tr>
          <th rowspan="2">
            <%= wicked_pdf_image_tag '//ics.thc.vc/logo-black.png', width: 180, height: 80 %><br>
            THC-ICS <%= controller_name.titleize %> Report
          </th>
          <th style="text-align: right;"><%= Time.now.strftime("%m/%d/%Y") %></th>
        </tr>

        <tr>
          
        </tr>
      </table>
    </section>

    <section id="report-body" class="col-md-10 col-md-offset-1">
      <%= yield %>
    </section>
  </body>
</html>