<%= content_for :head do %>
  <style>
    body {
      background: url( <%= label_stream_jar_path jar %> );
      background-size: cover;
      background-repeat: no-repeat;
    }

    h1 {
      font-size: 3em;
      
      text-align: right;
      padding-right: 100px;
      
      position: relative;
      top: 50%;
      -webkit-transform: translateY(-50%);
      -ms-transform: translateY(-50%);
      transform: translateY(-50%);
    }
  </style>
<% end %>

<h1><%= "JAR: #{jar.id}" %></h1>
