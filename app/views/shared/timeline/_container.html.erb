<%= render 'shared/timeline/container/ctn', ctn: container %>

<% if container.containers.present? then container.containers.each do |ctn| %>
  <%= render 'shared/timeline/container/ctn', ctn: ctn %>
<% end %>