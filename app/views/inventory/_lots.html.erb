<table class="table table-striped">
  <tr>
    <th>Name</th>
    <th>Strain</th>
    <th>Category</th>
    <th>Initial Weight (g)</th>
    <th>Current Weight (g)</th>
  </tr>

  <% lots.each do |lot| %>
    <tr>
      <td><%= lot.try(:to_s) if lot.present? %></td>
      <td><%= strains_for lot.try(:strains) if lot.present? %></td>
      <td><%= lot.try(:category) if lot.present? %></td>
      <td><%= weight_for lot.try(:initial_weight), '' if lot.present? %></td>
      <td><%= weight_for lot.try(:current_weight), '' if lot.present? %></td>
    </tr>
  <% end %>
</table>