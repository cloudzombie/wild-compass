<%= form_for(bag) do |f| %>
  <% if bag.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(bag.errors.count, "error") %> prohibited this bag from being saved:</h2>

      <ul>
      <% bag.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="form-group">
    <%= f.label :lot %>
    <%= f.collection_select :lot, Lot.all, :id, :name, :selected => params[:lot] %>
  </div>

  <div class="form-group">
    <%= f.label :initial_weight %>
    <%= f.text_field :initial_weight, class: 'form-control' %>
  </div>


  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
