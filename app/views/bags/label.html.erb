<script>
  $(document).ready(function() {
    var doc = new jsPDF('l', 'in', [3, 1]);
    
    doc.setFontSize(22);
    doc.text(1, 0.6, 'Bag : <%= bag.name %>');

    var imgData = "<%=j bag.datamatrix %>'"
    doc.addImage(imgData, 'PNG', 0.1, 0.1, 0.8, 0.8);
    
    doc.save('label.pdf');
  })
</script>